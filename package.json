{
  "name": "@nzbr/parcel-transformer-pandoc",
  "version": "1.0.0",
  "private": true,
  "source": "src/index.ts",
  "main": "dist/index.js",
  "scripts": {
    "prebuild": "rimraf ./dist",
    "build": "tsc -b",
    "prepare": "pnpm run build"
  },
  "engines": {
    "parcel": "2.x"
  },
  "dependencies": {
    "@parcel/plugin": "^2.8.3"
  },
  "peerDependencies": {
    "@parcel/types": "^2.8.3",
    "@types/node": "^18.11.18",
    "rimraf": "^5.0.0",
    "typescript": "^5.0.0"
  }
}
